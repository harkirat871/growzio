import './bootstrap';

import Alpine from 'alpinejs';

// Application timezone for any client-side date formatting (use with Intl, etc.)
window.APP_TIMEZONE = 'Asia/Kolkata';

window.Alpine = Alpine;

Alpine.start();
